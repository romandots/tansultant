version: '3'
services:
  app:
    build:
      context: ../manager-app
      dockerfile: DockerfileProduction
    restart: always
    depends_on:
      - api
  api:
    restart: always
    depends_on:
      - db
      - redis
  queue:
    restart: always
  scheduler:
    restart: always
  db:
    restart: always
  redis:
    restart: always